
pipeline {
    
    agent any
     
    stages {
        stage('build') {
            steps {
                sh 'node --version'
            }
        }
         stage('test') {
            steps {
                sh 'echo test'
            }
        }
    }
}
